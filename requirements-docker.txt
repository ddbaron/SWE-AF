# AgentField SWE Pipeline — Docker Dependencies
#
# Same runtime dependencies as requirements.txt.

# Profile propagation and the isolated OpenCode provider from the maintained
# AgentField fork; keep this commit aligned with pyproject.toml.
agentfield @ git+https://github.com/ddbaron/agentfield.git@aa0304577017ee25c405db2a7c2b5fd666f7decc#subdirectory=sdk/python
pydantic>=2.0
claude-agent-sdk==0.1.20
hax-sdk>=0.2.4
python-dotenv>=1.0
# cryptography 48.0.0 currently crashes with SIGILL on some Linux/aarch64
# Docker hosts when AgentField imports Ed25519 for DID registration.
cryptography<46
